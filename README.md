# М-Брокерс — сайт

ООО «Marishka Brok», таможенный представитель в Реестре ГТК РУз (разрешение №04-05/20-4360 от 23.08.2014).
Ташкент, Яшнабадский р-н, ул. Корасув, 4. Почта: info@raztamojka.uz, Telegram: @MB_Customs_Broker.

## Что готово

- `index.html` — главная. Собрана из макета Claude Design (`Home.dc.html`,
  проект «Marishka Brok сайт дизайн»).
- `about.html` — о компании: чем таможенный представитель отличается от посредника,
  документы, реквизиты, отзывы.
- `contacts.html` — контакты: телефон, Telegram, почта, реквизиты, карта проезда.
- `services.html` — оглавление услуг.
- `service-broker.html`, `service-clearance.html`, `service-certification.html`,
  `service-warehouse.html` — отдельная страница на услугу: что входит, шаги
  работы, что прислать, вопросы-ответы с микроразметкой FAQPage.
- `config.js` — **единственное место** для идентификатора GA4 и адреса
  обработчика заявок. Пусто — механика выключена, ничего не ломается.
- `analytics.js` — счётчик и цели: `call_click`, `telegram_click`,
  `email_click`, `form_submit` с указанием блока страницы.
- `form.js` + `relay/` — форма заявки и обработчик, кладущий лид в Telegram.
- `404.html` — с телефоном и разделами вместо пустой страницы GitHub.
- `uz/` — узбекская версия всех страниц (латиница).
- `fonts/` — самохостинг Oswald и Inter, по файлу на диапазон символов.
- `sticky.js` — плавающая шапка: клонирует `.topbar` и показывает копию,
  когда первый экран (`.hero` или `.pagehead`) ушёл наверх.
- `site.css` — общие стили всех страниц. **При правке стилей обязательно
  поднимайте версию** в `href="site.css?v=N"` во всех html: без этого
  у посетителя, который уже был на сайте, останется старый файл из кеша,
  и новые блоки приедут без оформления. Проверено на себе.

Двуязычный, ru в корне и uz в `/uz/`, связаны через `hreflang` и переключатель
RU/OZ в шапке. Без сборки и зависимостей, просто статика.

Шрифты Oswald (заголовки) и Inter (текст) лежат в `fonts/`, внешних origin нет.
**Archivo из макета не используется: у него нет кириллицы**, весь русский текст
рендерился системным шрифтом. Карта Яндекса (41.293607, 69.341031) грузится
по клику — виджет весит больше всей остальной страницы.

Картинки в webp с jpg-фолбэком, у героя три ширины через `srcset`.
Пересобирать после замены исходников — скриптом из истории коммита `a2bdff1`.

Секции главной: герой с шапкой и полосой фактов → «Что обычно ломает поставку»
(6 пунктов) → «Услуги» (4 строки) → «Не посредник, а таможенный представитель»
+ документы → «Клиенты» → «Офис» с картой → красный CTA-блок → подвал.

## Расхождение, которое нужно проверить у клиента

В сканах разрешения написано не то, что в тексте сайта:

| На сайте | В документе |
|---|---|
| Разрешение №04-05/20-4360 | № 0211 в Реестре таможенных брокеров |
| В Реестре с 23.08.2014 | от 21 апреля 2016 года |
| гл. 40 ст. 277 ТК РУз | статьи 281 и 282 ТК РУз |

Адрес в документах старый (Мирзо-Улугбекский р-н) — компания переехала,
актуальный адрес взят с raztamojka.uz. Сканы висят рядом с текстом,
цифры стоит сверить.

## Скорость

Первый экран главной на телефоне — 65,5 KB по проводу:

| | |
|---|---|
| html (gzip) | 5,2 KB |
| site.css (gzip) | 5,4 KB |
| sticky.js (gzip) | 1,6 KB |
| Oswald кириллица | 11,4 KB |
| Inter кириллица | 18,3 KB |
| герой webp 960 | 23,6 KB |

Было 364 KB плюс два внешних origin под шрифты и виджет Яндекса,
который грузился при открытии страницы.

## Ещё не сделано

Битых ссылок нет.

Осталось подключить, оба пункта — одна строка в `config.js`:

1. **GA4.** Вписать `ga4: 'G-XXXXXXXXXX'`. Цели уже размечены.
2. **Форма.** Развернуть воркер по инструкции в `relay/README.md`
   и вписать `formEndpoint`. Пока адрес пуст, форма скрыта на всех
   страницах — неработающая форма хуже, чем её отсутствие.

Разделы «Практика» и «Отзывы» из макета не свёрстаны, их пункты убраны
из меню. Макеты лежат в проекте Claude Design.

## Локальный просмотр

```bash
python3 -m http.server 4323 --directory /Users/zvezdotank/Claude/mbrokers
```
