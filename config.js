/* Единственное место, где живут внешние идентификаторы.
   Пока значение пустое — соответствующая механика просто выключена:
   форма не показывается, счётчик не грузится. Ничего не ломается. */
window.MB = {
  // Идентификатор Google Analytics 4, вида G-XXXXXXXXXX
  ga4: 'G-KRTJCLYWW3',

  // Адрес обработчика заявок (Cloudflare Worker), который кладёт лид
  // в Telegram. Токен бота живёт внутри воркера, на сайт не попадает.
  formEndpoint: ''
};
